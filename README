Python wrapper around Google's CityHash, non-cryptographic 64-bit and 128-bit
hash functions.  See http://code.google.com/p/cityhash/ for more information

Uses SWIG to generate wrapper.  See http://www.swig.org/ for more information

To install and run:

$ ./install.sh
$ python
>>> import city
>>> city.CityHash64('a',1)
2603192927274642682
